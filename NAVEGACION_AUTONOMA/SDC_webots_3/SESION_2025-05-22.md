# Sesión 2025-05-22 — Actividad 3.1 Detección de Peatones con SVM

## Estado al cierre

El auto sigue trabándose (beachball macOS) con `city_2025a_v2.wbt`.
El problema raíz identificado es doble:
1. **Freeze de Webots** — causa exacta no confirmada en esta sesión (ocurre incluso con `BRAKE_ON_DETECT = False`)
2. **SVM con domain gap** — modelo entrenado con fotos reales INRIA, no reconoce bien los modelos 3D de Webots → falsos positivos constantes

---

## Archivos clave y su estado actual

### Mundo activo
`worlds/city_2025a_v2.wbt`
- Basado en el original `city_2025a_activity_3_1.wbt`
- **4 peatones** (reducidos de 15 originales), speed=3.0 m/s, uno por cuadrante:
  - unnamed: trayectoria -31.8 33 → -31.8 55 (cuadrante -X/+Y)
  - pedestrian(6): 33 -57 → 33 -35 (cuadrante +X/-Y)
  - pedestrian(11): -21 -116 → -21 -94 (cuadrante -X/-Y sur)
  - pedestrian(14): 46 94 → 46 116 (cuadrante +X/+Y norte)
- Eliminados: OilBarrel, Supervisor, 11 peatones extra

### Controlador activo
`controllers/simple_controller_pedestrian.py`
- PID lane following (HSV yellow mask → Canny → ROI trapezoidal → HoughLinesP)
- SVM en hilo de fondo (threading.Thread, daemon=True), cada 10 frames
- **`BRAKE_ON_DETECT = False`** ← el auto NO frena, solo muestra label PEATON
  - Cambiar a `True` cuando el SVM esté reentrenado
- Parámetros PID: Kp=0.28, Ki=0.01, Kd=0.01, CRUISE_SPEED=30
- timestep = int(robot.getBasicTimeStep()) — sin multiplicador (crítico para evitar freeze)
- `ped_score`: solo cambia cuando llega resultado nuevo del hilo (+1 detección / reset 0 en negativo)
- Threshold: score >= 3 para confirmar peatón

### Mundo de recolección de datos (NUEVO, sin probar)
`worlds/collect_data.wbt`
- Mundo simple: fondo "noon_building_overcast" + piso gris + Supervisor robot con cámara
- Peatón DEF PED arranca en (200, 0, 200) — fuera de cámara
- Cámara: 320×240, FOV=0.84 rad, altura 1.2m
- **Pendiente: probar que el peatón aparezca correctamente y no freeze**

### Supervisor de captura (NUEVO, sin probar)
`controllers/collect_training_data.py`
- Teletransporta el peatón a 280 posiciones (7 distancias × 5 laterales × 8 rotaciones)
- Usa diferencia de imagen (frame con peatón − frame referencia) para localizar el crop automáticamente
- Guarda en `training_data/positive/` y `training_data/negative/`
- Al terminar llama `robot.simulationQuit(0)`

### Script de reentrenamiento (NUEVO, sin probar)
`../retrain_svm.py` (junto al .joblib)
- Carga `human detection dataset/0/` (1,274 negativos INRIA) + `human detection dataset/1/` (1,828 positivos INRIA)
- Agrega las capturas Webots de `training_data/`
- Entrena Pipeline(StandardScaler + SVC rbf), cross-validation F1
- Sobreescribe `pedestrian_svm.joblib`

---

## Problema del freeze — lo que sabemos

- El H3 controller (`semana_2/SIMULATOR1/simple_controller_H3.py`) corría sin freeze
- La clave que funcionó antes: `timestep = int(robot.getBasicTimeStep())` sin multiplicador
- El controlador actual ya usa esa configuración
- El freeze sigue ocurriendo — posibles causas no descartadas:
  - Número de peatones activos (4 × controlador Python cada timestep)
  - Threading del SVM generando contención de GIL en Python
  - Algún recurso específico del mundo city (tráfico, física de colisiones)

### Diagnóstico pendiente
Probar en este orden para aislar causa:
1. `test_minimal.py` con `city_2025a_v2.wbt` — ¿el mundo sin controlador SVM se traba?
2. `simple_controller_pedestrian.py` con `svm_model = None` (comentar el joblib.load) — ¿el PID solo se traba?
3. Si pasa 2 pero no 3 → el threading del SVM es el culpable

---

## Modelo SVM actual

**Archivo**: `pedestrian_svm.joblib`  
**Tipo**: `Pipeline(StandardScaler + SVC(kernel='rbf'))`  
**Features**: HOG 924-dim (orientations=11, pixels_per_cell=16×16, cells_per_block=2×2)  
**Entrenado con**: INRIA dataset (fotos reales)  
**Problema**: domain gap con los modelos 3D de Webots → falsos positivos constantes

---

## Próximos pasos (en orden)

### Paso 1 — Diagnosticar el freeze ANTES de todo lo demás
```
Webots → Abrir city_2025a_v2.wbt → cambiar controller del BMW a "test_minimal"
→ Si se traba: el problema es el mundo, no el controlador
→ Si no se traba: el problema es el controlador
```

### Paso 2 — Recolectar datos de entrenamiento Webots
```
Webots → Abrir collect_data.wbt → Play
→ Esperar ~2 min a que imprima "Listo — ejecuta retrain_svm.py"
→ Verificar que training_data/positive/ tenga 200+ imágenes
```

### Paso 3 — Reentrenar SVM
```bash
cd "NAVEGACION_AUTONOMA/Semana_5/Actividad 3.1 - Detección de Peatones con SVM"
python retrain_svm.py
```
Tarda ~3-5 min. Imprime CV F1 score al terminar.

### Paso 4 — Activar frenado y probar
En `controllers/simple_controller_pedestrian.py`, línea 69:
```python
BRAKE_ON_DETECT = True   # cambiar de False a True
```

---

## Parámetros de referencia del H3 (que funcionaba)

```python
# De semana_2/SIMULATOR1/simple_controller_H3.py
timestep = int(robot.getBasicTimeStep())   # sin multiplicador
CRUISE_SPEED = 30
Kp, Ki, Kd = 0.25, 0.005, 0.01
```

---

## Notas importantes

- **No usar `robot.getBasicTimeStep() * N`** — causa freeze en este mundo
- **No habilitar PointCloud** en el LiDAR — también causa freeze
- El LiDAR (`Sick LMS 291`) está completamente deshabilitado en el controlador actual — dejarlo así
- El mundo original sin modificar está en `city_2025a_activity_3_1.wbt` — nunca tocarlo
